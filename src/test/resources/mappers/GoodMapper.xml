<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.GoodMapper">
    <select id="getUserById" resultType="com.example.entity.AppUser">
        SELECT user_id, user_name, email FROM app_user WHERE user_id = #{userId}
    </select>
    <select id="getUserByName" resultType="com.example.entity.AppUser">
        SELECT user_id, user_name FROM app_user WHERE user_name = #{name}
    </select>
    <select id="getByEmail" resultType="com.example.entity.AppUser">
        SELECT user_id, user_name, email FROM app_user WHERE email = #{email}
    </select>
</mapper>
